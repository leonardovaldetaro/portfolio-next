import Styles from "./About.module.scss";

export default function About() {
    return (
        <section className={Styles.about}>
            <h1 className={Styles.about__title}>Sobre</h1>
        </section>
    );
}
